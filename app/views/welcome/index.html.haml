%nav.navbar.navbar-default.navbar-fixed-top{ role:'navigation'}
  .navbar-header
    = link_to root_path, class:'navbar-brand' do
      Tsuga

#map-container
  #map-canvas{ data: { 'clusters-path' => clusters_path } }

.container

:coffee
  $(document).ready ->
    console.log "doc ready"
    window.demo = new TsugaDemo '#map-canvas'
    demo.setup()